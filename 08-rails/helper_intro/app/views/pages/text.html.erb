<ul>
  <% @numbers.each do |n| %>
    <li style="background-color: <%= cycle('red', 'green', 'orange', 'purple') %>"><%= n %></li>
  <% end %>
</ul>

<p>You have <%= pluralize(@friend_count, 'friend') %>.</p>
<p>You have <%= pluralize(@enemy_count, 'enemy') %>.</p>

<p title="<%= @story %>"><%= truncate @story, :length => 15 %></p>
